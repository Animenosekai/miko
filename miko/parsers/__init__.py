"""
parsers

This folder contains a list of different documentation object parser
"""
from . import (changelog, copyright, deprecated, example, flag, important,
               inline, map, notes, parameters, parser, raises, references,
               returns, warnings, yields)
